def genprimes(n):
    primes[True]*(n+1)
    primes[0]=primes[1]=False
    for i in range(2,len(primes)):
        if primes[i]:
            for j in range(
t = int(input())
while t:
    print(genprimes(n))
    t=t-1
